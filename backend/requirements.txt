pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-dotenv>=1.0
# groq <= 0.11 is incompatible with httpx >= 0.28 (it passes a removed
# `proxies=` kwarg). app/groq_advisor.py works around it, but prefer a current SDK.
groq>=0.31
matplotlib>=3.7
seaborn>=0.13
